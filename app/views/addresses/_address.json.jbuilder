json.extract! address, :id, :calle, :entre, :entre2, :noExterior, :noInterior, :referencia, :state_id, :town_id, :towntype_id, :township_id, :codigo_postal_id, :created_at, :updated_at
json.url address_url(address, format: :json)
